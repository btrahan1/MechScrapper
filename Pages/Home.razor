@page "/"

<PageTitle>MechScrappers</PageTitle>

<div class="menu-container">
    <h1 class="title">MECH SCRAPPERS</h1>
    <h2 class="subtitle">BUILD. DRIVE. SURVIVE.</h2>

    <div class="menu-options">
        <a href="garage" class="menu-btn">ENTER GARAGE</a>
        <a href="wasteland" class="menu-btn secondary">INSTANT ACTION</a>
    </div>
</div>

<style>
    body { background: #111; color: #eee; }
    .menu-container {
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        height: 80vh;
        background-color: #000;
    }
    
    .title {
        font-family: 'Impact', sans-serif;
        font-size: 5rem;
        color: #fca311;
        text-shadow: 4px 4px 0px #14213d;
        margin-bottom: 0;
    }
    
    .subtitle {
        font-family: 'Courier New', monospace;
        letter-spacing: 5px;
        margin-bottom: 50px;
        color: #e5e5e5;
    }
    
    .menu-options {
        display: flex;
        flex-direction: column;
        gap: 20px;
    }
    
    .menu-btn {
        padding: 20px 40px;
        font-size: 1.5rem;
        text-decoration: none;
        color: #000;
        background: #fca311;
        border: 2px solid #fca311;
        font-weight: bold;
        text-transform: uppercase;
        font-family: sans-serif;
        transition: all 0.2s;
        text-align: center;
    }
    
    .menu-btn:hover {
        background: #e5e5e5;
        border-color: #e5e5e5;
        transform: scale(1.05);
    }
    
    .menu-btn.secondary {
        background: transparent;
        color: #e5e5e5;
        border-color: #e5e5e5;
    }
    
    .menu-btn.secondary:hover {
        background: #e5e5e5;
        color: #000;
    }
</style>
